def calc_distance(p1, p2):
    from math import sqrt
    x1, y1 = p1
    x2, y2 = p2
    return sqrt((x2 - x1)**2 + (y2 - y1)**2)


if __name__ == '__main__':
    # ??????????????\???
    data = [float(x) for x in input().split(' ')]

    # 2???????????¢????¨????
    result = calc_distance((data[0], data[1]), (data[2], data[3]))

    # ???????????????
    print('{0:.8f}'.format(result))