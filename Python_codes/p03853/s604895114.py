import sys
import numpy as np
# from decimal import Decimal
read = sys.stdin.buffer.read
readline = sys.stdin.buffer.readline
readlines = sys.stdin.buffer.readlines
# ----------------------------------------
h, w = map(int, readline().split())

data = []
for _ in range(h):
    #d0 = input().replace('*','1').replace('.','0')
    d0 = str(input())
    print(d0)
    print(d0)
